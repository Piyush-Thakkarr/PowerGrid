name: Keep Alive

on:
  schedule:
    - cron: '*/4 * * * *'  # Every 4 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s -o /dev/null -w "%{http_code}" https://owerrid-piyush-thakkarr8475-cydu1vj4.leapcell.dev/health
